apiVersion: v1
kind: Service
metadata:
  labels:
    run: keep-a-changelog
  name: keep-a-changelog
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: keep-a-changelog
  type: LoadBalancer
status:
  loadBalancer: {}